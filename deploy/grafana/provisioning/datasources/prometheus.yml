apiVersion: 1

# Datasource por default — apunta al container Prometheus de la misma red.
# Así no hace falta configurarlo desde la UI de Grafana.
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
